export enum paths {
  dashboard = '/dashboard',
  login = '/'
}

export const apiPaths = {
  account: {
    auth: '/auth/account',
    register: '/account'
  },
  driver: {
    auth: '/auth/driver',
    register: '/driver'
  },
  refreshToken: '/refresh-token'
};
